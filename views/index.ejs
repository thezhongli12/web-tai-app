<!DOCTYPE html>
<html>
<head>
    <title>Web Tải App</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="p-5 text-center">
    <div class="container">
        <% if (!role) { %>
            <div class="card p-4 mx-auto" style="max-width: 400px;">
                <h2>Đăng Ký</h2>
                <form action="/register" method="POST">
                    <input type="text" name="username" class="form-control my-3" placeholder="Nhập tên..." required>
                    <button class="btn btn-primary w-100">Xác Nhận</button>
                </form>
            </div>
        <% } else if (role === 'Admin') { %>
            <h1 class="text-danger">QUẢN TRỊ VIÊN</h1>
            <table class="table mt-4">
                <thead class="table-dark">
                    <tr><th>Tên Người Dùng</th><th>Vai Trò</th></tr>
                </thead>
                <tbody>
                    <% users.forEach(u => { %>
                        <tr><td><%= u.username %></td><td><%= u.role %></td></tr>
                    <% }) %>
                </tbody>
            </table>
            <a href="/" class="btn btn-secondary">Quay lại</a>
        <% } else { %>
            <h1>Chào <%= name %>!</h1>
            <p>Đăng ký thành công. Nhấn nút bên dưới để tải app.</p>
            <button class="btn btn-success btn-lg" onclick="alert('Đang tải...')">TẢI APP NGAY</button>
            <br><br><a href="/">Đăng ký tên khác</a>
        <% } %>
    </div>
</body>
</html>